language: java
jdk:
  - openjdk8

addons:
  sonarcloud:
    organization: "ispwrml"
    token:
      secure: "8002bef1a349aafa21eef3f3e3d19b5b975f7279" # encrypted value of your token

script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - ant build
  - sonar-scanner